# Vincere Mortem - Веб-сайт киберспортивной организации

Этот репозиторий содержит веб-сайт киберспортивной организации Vincere Mortem для хранения и публикации контента.

## 🚀 Технологии

- **React 19** - Библиотека для создания пользовательских интерфейсов
- **Vite** - Инструмент сборки и разработки
- **React Router DOM** - Маршрутизация
- **Decap CMS** - Система управления контентом
- **React Markdown** - Рендеринг Markdown контента
- **GitHub Pages** - Хостинг

## 📖 Демонстрация Markdown

Для ознакомления с возможностями форматирования Markdown в новостях, посмотрите пример:
👉 **[Демонстрация возможностей Markdown](https://vincere-mortem.pages.dev/news/content/oodid8)**

---

## 👥 Для редакторов контента

### Доступ к CMS (Content Management System)

1. Откройте админ-панель: `https://vincere-mortem.pages.dev/admin`
2. Войдите через GitHub аккаунт
3. После авторизации вы сможете создавать и редактировать контент

### Типы контента

#### 📰 Новости

Новости создаются в формате JSON и хранятся в `src/content/news/`.

**Поля:**
- **Заголовок** (title) - Заголовок новости
- **Дата публикации** (date) - Дата и время публикации (формат: YYYY-MM-DDTHH:mm:ss)
- **Обложка** (thumbnail) - Изображение обложки
- **Текст новости** (body) - Основной текст в формате Markdown
- **Категория** (category) - Категория новости

**Особенности:**
- Новости с датой публикации в будущем не отображаются на сайте до наступления указанного времени
- Новости с категорией `CONTENT` используются как справочные материалы и не отображаются в общем списке новостей
- Для форматирования текста используйте Markdown (см. демонстрацию выше)

**Форматирование Markdown:**
- Заголовки: `# Заголовок 1`, `## Заголовок 2`
- Жирный текст: `**текст**`
- Курсив: `*текст*`
- Списки: `- элемент` или `1. элемент`
- Ссылки: `[текст](URL)`
- Изображения: `![alt](URL)`
- И другое - см. [демонстрацию](https://vincere-mortem.pages.dev/news/content/oodid8)

#### 🎮 Матчи

Матчи хранятся в `src/content/matches/`.

**Поля:**
- **Турнир** (tournament) - Название турнира
- **Соперник** (opponent) - Название команды соперника
- **Дата и время** (date) - Дата и время матча
- **Лого соперника** (logo) - Логотип команды соперника (опционально)
- **Дисциплина** (discipline) - Название игры
- **Счет** (score) - Результат матча, если он уже сыгран (ВАЖНО! На странице матчей публикуются прошедшие матчи, если не добавлять счет, матч опубликован не будет.)
- **Ссылка на стрим** (stream) - URL стрима
- **Место проведения** (venue) - Место проведения (по умолчанию: `Онлайн`, однако может быть изменен на настоящее место проведения или LAN)
- **Статус** (status) - Статус матча: `upcoming`, `live`, `finished` (по умолчанию: `upcoming`)

#### 👥 Команды (недоступно в CMS так как не подвергается частым изменениям)

Команды хранятся в `src/content/teams/`.

**Структура команды:**
- **name** - Название команды
- **discipline** - Дисциплина
- **image** - Изображение команды
- **description** - Описание
- **players** - Массив игроков
  - **nickname** - Никнейм игрока
  - **name** - Имя (инициалы)
  - **fullName** - Полное имя
  - **country** - Страна (код из двух букв)
  - **role** - Роль
  - **avatar** - Аватар игрока
- **achievements** - Массив достижений
- **seasonStats** - Статистика сезона (объект с ключами: Matches, Wins, Losses, Winrate, Avg Damage, K/D Ratio)

### Загрузка изображений

Все изображения (кроме автаров) загружаются в `public/images/uploads/` и автоматически доступны по пути `/images/uploads/имя_файла.jpg`, аватары по пути `public/images/avatars/`

**Рекомендуемые размеры изображений:**

#### 📰 Обложки новостей
- **Рекомендуемый размер:** 1280px × 1280px
- **Формат:** JPG, PNG, WebP
- **Использование:**
  - На странице деталей новости: отображается на всю ширину экрана с высотой 600px
  - На странице списка новостей: высота 380px
  - На главной странице: 620px или 300px
- **Рекомендация:** Используйте изображения высокого качества, так как они автоматически масштабируются и обрезаются через `object-fit: cover`

#### 👥 Изображения команд
- **Рекомендуемый размер:** 1200px × 250px
- **Формат:** JPG, PNG, WebP
- **Использование:**
  - На странице деталей команды: отображается на всю ширину с высотой 400px
  - На странице списка команд: полная ширина с минимальной высотой 250px
- **Рекомендация:** Изображения автоматически конвертируются в градации серого для единого стиля

#### 👤 Аватары игроков
- **Рекомендуемый размер:** 140px × 140px
- **Формат:** JPG, PNG, WebP (квадратные изображения)
- **Использование:**
  - На странице деталей команды: отображаются как 90px × 90px
  - На странице списка команд: отображаются как 50px × 50px (36px на мобильных)
- **Рекомендация:** Квадратные изображения с центральной композицией, так как они обрезаются до круга

#### 🎮 Логотипы соперников
- **Рекомендуемый размер:** 200px × 200px
- **Формат:** PNG или JPG (с прозрачным фоном)
- **Использование:**
  - На странице матчей: отображаются как 50px × 50px (40-45px на мобильных)
  - В блоке "Next Match" на главной: 100px × 100px (60-80px на мобильных)
- **Рекомендация:** Квадратные изображения, желательно с прозрачным фоном

**Общие рекомендации:**
- Оптимизируйте изображения перед загрузкой для уменьшения размера файла
- Используйте форматы WebP или JPG для фотографий (меньший размер файла)
- Используйте PNG для логотипов и изображений с прозрачностью
- Максимальный рекомендуемый размер файла: 500KB на изображение

---

## 👨‍💻 Для разработчиков

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd cyber-project
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите сервер разработки:**
   ```bash
   npm run dev
   ```

4. **Откройте браузер:**
   Сайт будет доступен по адресу `http://localhost:5173`

### Доступные команды

- `npm run dev` - Запуск сервера разработки
- `npm run build` - Сборка проекта для продакшена
- `npm run preview` - Предпросмотр продакшена сборки
- `npm run lint` - Проверка кода линтером

### Структура проекта

```
cyber-project/
├── public/                # Статические файлы
│   ├── admin/             # CMS админ-панель
│   │   ├── config.yml     # Конфигурация CMS
│   │   └── index.html     # HTML для CMS
│   └── images/            # Изображения
│       ├── uploads/       # Загруженные изображения
│       └── avatars/       # Аватары игроков
├── src/
│   ├── assets/            # Ресурсы (шрифты, изображения)
│   ├── components/        # React компоненты
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   ├── Layout.jsx
│   │   └── ScrollToTop.jsx
│   ├── content/           # Контент сайта
│   │   ├── news/          # JSON файлы новостей
│   │   ├── matches/       # JSON файлы матчей
│   │   └── teams/         # JSON файлы команд
│   ├── contexts/          # React Contexts
│   │   └── NewsContext.jsx
│   ├── pages/             # Страницы приложения
│   │   ├── Home.jsx
│   │   ├── News.jsx
│   │   ├── NewsDetail.jsx
│   │   ├── Matches.jsx
│   │   ├── Teams.jsx
│   │   ├── TeamDetail.jsx
│   │   └── About.jsx
│   ├── styles/            # CSS стили
│   ├── utils/             # Утилиты
│   ├── App.jsx            # Главный компонент приложения
│   └── main.jsx           # Точка входа
├── functions/             # Cloudflare Functions (для CMS)
│   └── api/
│       ├── auth.js
│       └── callback.js
├── package.json
├── vite.config.js
└── README.md
```

### Архитектура

#### Загрузка контента

Контент загружается динамически через Vite `import.meta.glob`:

- **Новости**: `src/content/news/*.json`
- **Матчи**: `src/content/matches/*.json`
- **Команды**: `src/content/teams/*.json`

#### Фильтрация новостей

- Новости с датой в будущем не отображаются
- Новости с `category: "CONTENT"` не отображаются в общем списке, но доступны по прямому URL
- Новости сортируются по дате (новые первыми)

#### Маршрутизация

- `/` - Главная страница
- `/news` - Список новостей
- `/news/:id` - Детальная страница новости (по индексу)
- `/news/content/:slug` - Детальная страница контент-новости (по slug)
- `/matches` - Список матчей
- `/teams` - Список команд
- `/teams/:id` - Детальная страница команды
- `/about` - О нас
- `/shop` - Магазин (в разработке)
- `/admin` - Админ-панель CMS

#### Markdown рендеринг

Для рендеринга Markdown используется `react-markdown` с плагинами:
- `remark-gfm` - Поддержка GitHub Flavored Markdown
- `rehype-raw` - Поддержка HTML в Markdown

### CMS (Decap CMS)

CMS настроена для работы с GitHub через OAuth.

**Конфигурация:** `public/admin/config.yml`

**Backend:** GitHub
- Репозиторий: `xBRANtX/web-vincere-mortem`
- Ветка: `main`

**Аутентификация:** Через Cloudflare Functions (`functions/api/auth.js` и `functions/api/callback.js`)

### Деплой

Сайт развернут на Cloudflare Pages:
- **URL:** https://vincere-mortem.pages.dev
- При пуше в `main` ветку происходит автоматическая сборка и деплой

### Разработка компонентов

При создании новых компонентов:
1. Создайте компонент в `src/components/`
2. Добавьте стили в `src/styles/`
3. Используйте существующие стили как референс
4. Проверьте адаптивность на разных устройствах

### Добавление новых типов контента

1. Создайте папку в `src/content/`
2. Добавьте JSON файлы с данными
3. Обновите `src/App.jsx` для загрузки нового контента
4. Создайте страницу в `src/pages/`
5. Добавьте маршрут в `src/App.jsx`
6. (Опционально) Добавьте коллекцию в `public/admin/config.yml` для редактирования через CMS

---

## 📝 Примечания

- Все даты должны быть в формате ISO 8601 (например: `2026-01-13T22:00:00.000+03:00`)
- Изображения должны быть оптимизированы перед загрузкой
- При изменении структуры JSON файлов, убедитесь, что все существующие файлы обновлены
- Перед коммитом запускайте `npm run lint` для проверки кода

## 🔗 Полезные ссылки

- [Демонстрация Markdown](https://vincere-mortem.pages.dev/news/content/oodid8)
- [Сайт проекта](https://vincere-mortem.pages.dev)
- [Decap CMS документация](https://decapcms.org/docs/)